---
const LINKS = {
  kyte: 'https://fressia-perfumeria.kyte.site/es',
  whatsapp: 'https://wa.me/message/ES7WVJHY4MUYB1',
  instagram: 'https://www.instagram.com/fressiaperfumeria?igsh=MXhydG45N2p6Y3RmYg==',
  facebook: 'https://www.facebook.com/share/167JrGAFts/?mibextid=wwXIfr',
  tiktok: 'https://www.tiktok.com/@fressia.perfumeria?_t=ZS-8zjnOz7ox1U&_r=1',
  maps: 'https://maps.app.goo.gl/ZB598vwRSKuRnhf58?g_st=ic',
};
---

<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Fressia Perfumería & Accesorios</title>
  <meta name="description" content="Fragancias premium, réplicas 1.1 y perfumes por onzas. Joyería, gorras y más." />
  <link rel="icon" href="/logo-fressia.png" />
  <style>
    :root{
      --bg:#0c0c0f; --panel:#111117; --edge:#23232c;
      --text:#efeff4; --muted:#a8a8b3;
      --gold:#d4af37; --gold2:#a88928;
      --rad:18px; --shadow:0 20px 60px rgba(0,0,0,.55);
    }
    *{box-sizing:border-box}
    html,body{margin:0;background:#caa864; background:linear-gradient(180deg,#caa864 0,#b89259 14%,#b89259 100%);}
    body{color:var(--text);font-family:Inter, ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial}
    a{color:inherit;text-decoration:none}

    .wrap{max-width:1220px;margin:32px auto;padding:0 20px}
    .board{
      background:var(--panel);
      border-radius:24px;
      box-shadow:var(--shadow);
      overflow:hidden;
      position:relative;
    }

    /* barra superior */
    .top{
      display:flex; align-items:center; justify-content:space-between;
      padding:18px 24px; border-bottom:1px solid var(--edge);
    }
    .brand{display:flex; gap:12px; align-items:center}
    .brand img{width:36px;height:36px;object-fit:contain;filter:drop-shadow(0 6px 18px rgba(0,0,0,.6))}
    .brand b{letter-spacing:.5px}
    nav{display:flex; gap:24px; color:var(--muted)}
    nav a:hover{color:#fff}

    /* HERO */
    .hero{
      display:grid; grid-template-columns:1.1fr .9fr; gap:24px;
      padding:40px 28px 30px;
      position:relative;
    }
    .hero:before{
      /* cinta dorada superior derecha */
      content:""; position:absolute; right:40px; top:26px;
      width:140px; height:18px; border-radius:6px;
      background:linear-gradient(90deg,var(--gold2),var(--gold),var(--gold2));
    }
    .left h1{
      font-family: "Georgia", "Times New Roman", serif;
      font-size: clamp(36px,5vw,64px);
      margin:8px 0 12px;
      line-height:1.03;
      color:#f6f6f8;
    }
    .subtitle{color:var(--muted); max-width:700px; font-size:clamp(15px,2.2vw,18px)}
    .cta{display:flex; gap:14px; flex-wrap:wrap; margin-top:22px}
    .btn{
      display:inline-flex; gap:10px; align-items:center; font-weight:700;
      padding:14px 18px; border-radius:12px; border:1px solid var(--gold2);
      background:linear-gradient(180deg,var(--gold),var(--gold2));
      color:#111; box-shadow:var(--shadow); transition:transform .15s ease, filter .15s ease;
    }
    .btn:hover{transform:translateY(-1px); filter:saturate(1.05)}
    .btn.ghost{
      background:transparent; color:#fff; border:1px solid var(--edge)
    }
    .btn.ghost:hover{border-color:var(--gold); color:#fff}

    /* tarjeta derecha imagen */
    .photo{
      position:relative; border-radius:16px; overflow:hidden;
      border:1px solid var(--edge); background:#0f0f13;
      min-height:340px;
    }
    .photo img{display:block; width:100%; height:100%; object-fit:cover}
    .discount{
      position:absolute; left:-18px; bottom:-18px;
      width:150px; height:150px; border-radius:999px;
      background: radial-gradient( circle at 30% 30%, #fff 0, #fff 52%, transparent 53% ),
                  conic-gradient(from 0deg, var(--gold), var(--gold2), var(--gold));
      box-shadow:var(--shadow);
      display:grid; place-items:center;
    }
    .discount > div{
      width:118px; height:118px; border-radius:999px; background:#fff; color:#222; display:grid; place-items:center;
      text-align:center; font-weight:800; font-size:26px; line-height:1.05;
      box-shadow: inset 0 0 0 8px rgba(212,175,55,.25);
    }
    .discount small{display:block; color:#555; font-weight:700; font-size:12px; letter-spacing:.4px; margin-top:2px}

    /* decoraciones */
    .dec, .dec2{position:absolute; background:#fff}
    .dec{width:22px;height:3px;left:28px;top:120px}
    .dec:after{content:""; position:absolute; width:22px; height:3px; top:9px; background:#fff}
    .dec2{right:24px;top:80px;width:4px;height:24px;background:#fff}

    /* cards */
    .features{
      display:grid; grid-template-columns:repeat(3,1fr); gap:18px; padding:20px 28px 32px;
    }
    .card{
      background:#0f0f13; border:1px solid var(--edge); border-radius:14px; padding:16px 16px;
      color:#ddd; box-shadow:var(--shadow)
    }
    .card b{color:#fff}
    .foot{
      display:flex; justify-content:space-between; align-items:center; padding:14px 24px; border-top:1px solid var(--edge);
      color:var(--muted)
    }
    .social{display:flex; gap:12px; align-items:center}
    .chip{width:30px;height:30px;border-radius:999px;display:grid;place-items:center;background:#1a1a22;border:1px solid var(--edge)}
    .chip:hover{border-color:var(--gold)}

    @media (max-width:920px){
      .hero{grid-template-columns:1fr}
      .discount{left:auto; right:12px; bottom:-18px}
      .features{grid-template-columns:1fr}
    }
  </style>
</head>
<body>
  <div class="wrap">
    <section class="board">
      <!-- Top bar -->
      <div class="top">
        <div class="brand">
          <img src="/logo-fressia.png" alt="Fressia Perfumería" />
          <b>Fressia Perfumería</b>
        </div>
        <nav>
          <a href={LINKS.kyte} target="_blank" rel="noopener">Catálogo</a>
          <a href={LINKS.whatsapp} target="_blank" rel="noopener">WhatsApp</a>
          <a href={LINKS.maps} target="_blank" rel="noopener">Cómo llegar</a>
        </nav>
      </div>

      <!-- HERO -->
      <div class="hero">
        <div class="left">
          <div class="dec"></div>
          <h1>Skin Balance</h1>
          <p class="subtitle">
            Fragancias con carácter y balance perfecto. Réplicas 1.1, por onzas y colecciones árabes.
            <br/>Granada (Meta) & Villavicencio · Envíos a todo el país.
          </p>
          <div class="cta">
            <a class="btn" href={LINKS.kyte} target="_blank" rel="noopener">Ver catálogo</a>
            <a class="btn ghost" href={LINKS.whatsapp} target="_blank" rel="noopener">Pedir por WhatsApp</a>
          </div>
        </div>

        <div class="photo">
          <img src="/hero-bottle.jpg" alt="Fragancia Fressia" />
          <div class="discount"><div>25%<small>Descuento</small></div></div>
          <div class="dec2"></div>
        </div>
      </div>

      <!-- Features -->
      <div class="features">
        <div class="card"><b>Réplicas 1.1</b><br/>Calidad top y fijación superior.</div>
        <div class="card"><b>Por onzas</b><br/>Decants 5/10/30/60/100 ml.</div>
        <div class="card"><b>Accesorios & más</b><br/>Joyería 18K laminado, gorras, gafas, camisetas.</div>
      </div>

      <!-- Footer -->
      <div class="foot">
        <span>© {new Date().getFullYear()} Fressia Perfumería · Club VIP</span>
        <div class="social">
          <a class="chip" href={LINKS.instagram} target="_blank" aria-label="Instagram">IG</a>
          <a class="chip" href={LINKS.facebook} target="_blank" aria-label="Facebook">f</a>
          <a class="chip" href={LINKS.tiktok} target="_blank" aria-label="TikTok">TT</a>
          <a class="chip" href={LINKS.maps} target="_blank" aria-label="Maps">📍</a>
        </div>
      </div>
    </section>
  </div>
</body>
</html>



